echo "🚀 [Harness Engine] Đang kiểm tra staged files..."

# Chạy lint-staged để kiểm tra các tệp staged mà không sửa tự động hoặc typecheck toàn dự án
bunx lint-staged || exit 1

echo "✅ [Harness Engine] Kiểm tra staged files thành công! Cho phép commit."
